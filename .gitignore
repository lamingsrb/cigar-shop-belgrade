node_modules/
dist/
.vite/
.DS_Store
.env
.env.local
*.log
# Lokalni .npmrc sa Windows os override — CI Linux ne sme da vidi
.npmrc

.vercel

# Lokalni fajlovi sa poverljivim podacima (nikad u git)
CREDENTIALS.local.md
*.local.md
*.secret

# Build script intermediates
scripts/.tmp-*/

# Local IDE/editor configs
.vscode/

# Feedback raw client assets — keep only docs (FEEDBACK-ANA.md, TASKS.md) in git.
# Sirovi MP4/ZIP/raw materijali su lokalni; web koristi optimizovane verzije iz public/assets/.
Feedback_loop/*/Media/*.mp4
Feedback_loop/*/Media/*.zip
Feedback_loop/*/Media/Kubanske_cigare_unzipped/
.git-commit-msg.tmp

# Hosting / domain credentials + handover docs — NEVER commit (PDF reference credentials)
Hosting_Setup/

# Recolor backups (keeping original safe locally)
public/assets/brand/*.original.png

# Raw feedback media — only docs (FEEDBACK-*.md, TASKS.md) idu u git
Feedback_loop/*/Media/
Thumbs.db
